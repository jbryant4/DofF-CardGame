import createComponent from '~/utils/styles/createComponent';

export const Container = createComponent('div', { className: '' });

export const Wrapper = createComponent('div', { className: '' });

export const FilterTab = createComponent('div', { className: '' });
export const SubFilterWrapper = createComponent('div', { className: '' });
export const SubFilter = createComponent('div', { className: '' });

import { useState } from 'react';

type OwnProps = {};

const RockPaperScissors = ({}: OwnProps) => {
  const [value, setValue] = useState();

  return (
    <div>
      <div>RockPaperSiccors</div>
    </div>
  );
};

export default RockPaperScissors;

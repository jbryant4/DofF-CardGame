const sortClassName = require('prettier-plugin-sort-class-names');

module.exports = {
  plugins: [sortClassName],
  arrowParens: 'avoid',
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'none'
};
